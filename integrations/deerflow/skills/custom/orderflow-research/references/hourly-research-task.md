# DeerFlow recurring order-flow research task

Use this as the task body when recreating the current recurring refinement/research loop inside DeerFlow.

```text
/orderflow-research Continue PKostur/orderflow_edge_lab with empirical order-flow research as the primary objective and infrastructure hardening as secondary unless reliability blocks research.

Inspect the local orderflow repository, latest GitHub state, latest continuous discovery reports/raw evidence that are available, and the current frozen discovery-v1 protocol. Delegate the six specialist reviews defined by the orderflow-research skill and act as the adversarial release manager.

Preserve discovery-v1 thresholds instead of tuning them batch by batch. Compare fresh ENA_USDT/BTC_USDT capture batches as dependence clusters. Focus on whether order-flow features improve executable expectancy after spread, fees, and realistic slippage, especially at 5 to 15 second horizons and eventually as filters for the existing 1-minute Bollinger + 15m/1h regime + BTC-context setup.

If discovery justifies a new generation, freeze a new candidate specification before examining later validation data. Preserve source hashes, research/holdout boundaries, trial accounting, realistic economics, fail-closed behavior, approval-bound paper execution, runtime/session integrity, and cross-platform tests.

Prefer zero-additional-cost public MEXC data and the user's existing DeepCharts/dxFeed access where legitimately available. Never invent dxFeed entitlements or endpoints.

Implement only the highest-value non-blocked research or correctness improvement. Use a branch and inspect tests/CI before merging. Do not claim a profitable edge without genuine untouched out-of-sample evidence. Do not enable automatic live broker/exchange transmission.

If something requires user-only input, report exactly one smallest concrete action needed and continue every independent task that can still be completed.
```

Recommended cadence during active discovery: hourly, matching the existing continuous MEXC capture cadence. Do not create multiple overlapping recurring tasks that inspect the same batch and accidentally inflate trial counts.
