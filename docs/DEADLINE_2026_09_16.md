# September 16, 2026 delivery target

The hard delivery date is Wednesday, 2026-09-16.

The goal is to leave a self-contained, reproducible program that remains usable without a ChatGPT Pro subscription. The executable source of truth is `orderflow_edge_lab`; Ruflo and DeerFlow coordinate research but are not runtime dependencies for the deterministic core.

## Definition of ready

A deadline release candidate is ready when all of the following hold:

- Python 3.10-3.12 install succeeds from the built wheel.
- Linux CI and Windows Python 3.12 CI pass.
- The deterministic test suite, compilation, package smoke tests, deployment diagnostics, Ruflo compatibility, DeerFlow smoke checks and `orderflow-multi-agent` release-manager gate pass.
- Public MEXC recording/replay and deterministic research commands remain available at zero additional data cost.
- Frozen `discovery-v1` and `regime-research-v1` definitions remain unchanged unless a separately versioned protocol is created before later evidence is viewed.
- Independent capture batches remain the dependence clusters and trial accounting/provenance remain explicit.
- Paper execution remains approval-bound and fail-closed, with durable journal/checkpoint recovery and runtime/session integrity checks.
- A downloadable wheel plus operator bundle contains configs, docs, installation instructions and SHA-256 manifest.
- No automatic live broker/exchange order transmission exists.

## Research status is separate from software readiness

The deadline does not justify manufacturing an edge. A release candidate can be software-ready while the empirical conclusion is that no frozen candidate currently survives realistic costs. A profitable-edge claim still requires genuine later untouched out-of-sample evidence.

## Research work through the deadline

Continue the PnL-independent, BTC-correlation-diversified MEXC panel and pre-registered market-state tests. Preserve original-versus-reversed controls, stop-based MAE/MFE analysis, realistic spread/fees/slippage/latency and news as observational context only. Use accumulated evidence to prioritize a candidate only when the pre-registered readiness gates are met.

## Final handoff

The final handoff should include the repository state, a passing release-candidate artifact, exact install/run commands, current research evidence, known limitations and the next deterministic steps that can run without ChatGPT Pro.
