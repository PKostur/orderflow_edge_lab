name: DV2 Range Shock Momentum D4 Shadow - RETIRED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Candidate terminal status
        run: |
          echo "dv2_liquidity_range_shock_momentum_30d_v1 is retired."
          echo "Reason: frozen non-overlapping Binance 2025 D3 temporal holdout failed."
          echo "The pre-result D4 boundary remains audit evidence only and must not rescue the dead candidate ID."
