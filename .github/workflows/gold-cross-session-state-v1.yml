name: Gold Cross Session State v1

on:
  workflow_dispatch:
  push:
    branches: [research/gold-cross-session-state-v1]
    paths:
      - "config/gold_cross_session_state_v1.json"
      - "scripts/run_gold_cross_session_state_v1.py"
      - ".github/workflows/gold-cross-session-state-v1.yml"

permissions:
  contents: read

concurrency:
  group: gold-cross-session-state-v1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  state-study:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install runtime
        run: python -m pip install pandas numpy
      - name: Fetch pinned XAUUSD M15 data
        run: |
          set -euo pipefail
          mkdir -p artifacts/gold_cross_session_state_v1/data
          url="https://raw.githubusercontent.com/simom1/XAUUSD-history/bda79c6c5ce09a5b488d4cd2c20fcd19bdd2ed3a/Gold-Cash/XAUUSD/XAUUSD_M15_2010_2026.csv"
          curl --fail --location --retry 3 --retry-delay 2 "$url" \
            -o artifacts/gold_cross_session_state_v1/data/XAUUSD_M15.csv
          sha256sum artifacts/gold_cross_session_state_v1/data/XAUUSD_M15.csv \
            > artifacts/gold_cross_session_state_v1/data/SHA256SUMS.txt
      - name: Run frozen state study
        run: |
          python scripts/run_gold_cross_session_state_v1.py \
            --config config/gold_cross_session_state_v1.json \
            --data artifacts/gold_cross_session_state_v1/data/XAUUSD_M15.csv \
            --output artifacts/gold_cross_session_state_v1/result.json
      - name: Guard research boundary
        run: |
          python - <<'PY'
          import json
          r=json.load(open('artifacts/gold_cross_session_state_v1/result.json'))
          assert r['protocol'] == 'gold_cross_session_state_v1'
          assert r['grid']['hypotheses'] == 10
          assert r['pnl_tested'] is False
          assert r['locked_internal_validation_opened'] is False
          assert r['retrospective_extension_opened'] is False
          assert r['leverage_tested'] is False
          assert r['claims']['verified_future_oos'] is False
          assert r['claims']['profitable_edge_established'] is False
          assert r['claims']['leverage_authorized'] is False
          assert r['claims']['live_enabled'] is False
          PY
      - name: Preserve evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gold-cross-session-state-v1
          path: artifacts/gold_cross_session_state_v1/
          retention-days: 30
          if-no-files-found: warn
